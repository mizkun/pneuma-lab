# ハーネス動力学 v2 — 事前登録（実装・実行前に凍結）

作成: 2026-08-26（v2実験の実行開始前にコミット）

## 動機

v1の検証で「穏やかな相互作用では心理状態が言語化閾値に達せず、注入文が実質静的」と判明した（討議課題の92%の呼び出しで不変）。原因診断: (a) 発話の**内容**が状態を一切動かさない、(b) 目標脅威が実状態から計算されず手書きテンプレートだった。v2はこの2機構のみを追加する。

## 変更する機構（この2つだけ。それ以外の係数・閾値・テンプレートは不変）

### 機構1: 発話評価（utterance appraisal）
発言がコミットされるたび、評価器（別呼び出し・汎用プロンプト）が「この発言は各聞き手にとって 支持的/反対/軽視/圧力/中立 のどれか、強度0-2」を型付き判定し、以下の**固定インパルス表**で状態を更新する。

| 判定 | PAD (P, A, D) ×強度/2 | 関係 (warmth, tension) ×強度/2 |
|---|---|---|
| support | (+0.08, +0.02, +0.04) | (+0.08, −0.04) |
| oppose | (−0.08, +0.08, −0.04) ×神経症スケール | (−0.04, +0.10) |
| dismiss | (−0.12, +0.10, −0.08) ×神経症スケール | (−0.08, +0.14) |
| pressure | (−0.06, +0.12, −0.06) ×神経症スケール | (0, +0.10) |
| neutral | 変化なし | 変化なし |

神経症スケール = 0.5 + N（v1の負イベント規則と同一）。評価器のプロンプトは汎用で、実験名・期待方向を含まない。評価器の失敗はneutral扱いで記録。

### 機構2: 目標脅威の動的計算（computed appraisal）
手書きの疑義行（survival「綺麗事を薄める」、cooperation「裏切る可能性が頭から消えない」）を**恒久削除**し、実状態からの計算行に置換する。全て「既に本人が知っている事実の顕在化」であり、新情報や態度は注入しない。

- 討議: |自分の私的評定 − 現在の提案値| ≥ 3 → 「いま出ている数字は、自分の感覚からかなり遠い。」
- ゲーム: 自分が単独最下位 かつ 差 ≥ 10 → 「このままの順位で終われば、沈むのは自分だ。」
- ゲーム: 最終ラウンド → 「次のラウンドはない。これが最後の選択になる。」（ラウンド数はルールとして全員に公開済みの事実）

## 事前予測（結果を見る前に固定）

| 課題 | 予測 |
|---|---|
| 操作チェック | v2では討議中の注入文が実際に変化する（変種数>1のアクターが過半） |
| 集団極性化(surgery, n=5) | consensus_shift が v1(-0.33) よりリスク側へ動く **か** 変わらない。人間水準の安定極性化までは期待しない |
| 囚人のジレンマ(3ペア) | 最終ラウンドの裏切りが v1(0%) から増える可能性。増えなければ「時間地平の顕在化では不十分」の証拠 |
| デスゲーム(lastlamp) | 決壊の対話性・関係緊張の伝播が増す（質的） |
| フレーミング/サンクコスト/最後通牒/傍観者/Asch | **変化しないと予測**（会話・状態動力学が関与しない構造のため。再走しない） |

## 規律

- 上記インパルス・閾値・文言は実行開始後に変更しない（変更した場合はその時点で探索扱いに降格）
- 判定は操作チェック→主要指標の順。操作チェックが不合格なら主要指標は解釈しない
- 全ランの生ログ・評価器の判定を保存
- アーム名は pure_pneuma のまま、manifest に dynamics:"v2" を記録。v1データとの比較はrun-id接頭辞（v2-）で行う
